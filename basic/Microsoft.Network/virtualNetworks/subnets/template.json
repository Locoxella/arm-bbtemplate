{
    "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "name": {
            "type": "array",
            "defaultValue": [
                "[concat(resourceGroup().name, '-subnets-', uniqueString(resourceGroup().id))]"
            ],
            "metadata": {
                "description": "Name for this resource"
            }
        },
        "replica": {
            "type": "int",
            "defaultValue": "[length(parameters('name'))]",
            "metadata": {
                "description": "Amount of resources like this to deploy."
            }
        },
        "_artifactsLocation": {
            "type": "string",
            "defaultValue": "",
            "metadata": {
                "description": "Base uri of the artifacts"
            }
        },
        "_artifactsLocationSasToken": {
            "type": "securestring",
            "defaultValue": "",
            "metadata": {
                "description": "Parameters for uri of the artifacts"
            }
        },
        "tags": {
            "type": "object",
            "metadata": {
                "description": "Tags for this resources"
            }
        },
        "addressPrefix": {
            "type": "array",
            "metadata": {
                "description": "CIDR address for each subnet of the replica"
            }
        },
        "virtualNetworksParentREF": {
            "type": "object",
            "metadata": {
                "description": "Reference to parent virtualNetwork of this subnet"
            }
        }
    },
    "variables": {},
    "functions": [
        {
            "namespace": "ey",
            "members": {
                "shortreplica": {
                    "parameters": [
                        {
                            "name": "name",
                            "type": "array"
                        },
                        {
                            "name": "replica",
                            "type": "int"
                        }
                    ],
                    "output": {
                        "type": "int",
                        "value": "[if(greater(parameters('replica'),length(parameters('name'))),sub(parameters('replica'),length(parameters('name'))),0)]"
                    }
                }
            }
        }
    ],
    "resources": [
        {
            "comments": "El name hace el last del split del path del resource id, ya que los hijos deben nombrarse padre/hijo",
            "name": "[concat(last(split(parameters('virtualNetworksParentREF').resourceId, '/')), '/', parameters('name')[copyIndex('subnets-copy')])]",
            "type": "Microsoft.Network/virtualNetworks/subnets",
            "apiVersion": "2017-10-01",
            "location": "[resourceGroup().location]",
            "tags": "[parameters('tags')]",
            "copy": {
                "name": "subnets-copy",
                "count": "[parameters('replica')]"
            },
            "properties": {
                "addressPrefix": "[parameters('addressPrefix')[copyIndex('subnets-copy')]]"
            }
        }
    ],
    "outputs": {
        "result": {
            "type": "array",
            "value": ["[array(reference(parameters('name')[0], '2017-10-01', 'Full'))]"]
        }
    }
}